import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundSplash = Color(0xFF141414);
    static const Color background = Color(0xFF0E0E10);

  static const Color card = Color(0xFF1F1F1F);
  static const Color accent = Color(0xFFFF4B4B);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.white54;
  static const Color textDisabled = Colors.white24;
}